# Copy file này thành `.env` ở cùng thư mục (frontend/), Vite sẽ tự load.
# Mọi biến muốn dùng trong code React PHẢI có prefix VITE_ (Vite expose biến qua import.meta.env).

VITE_API_BASE_URL=http://localhost:8080/api
